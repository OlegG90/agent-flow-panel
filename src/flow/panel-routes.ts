/** Paths the panel serves, shared by the server, the renderer and the client. */
export const FLOW_CONTAINER_ID = "flow"
export const EVENTS_PATH = "/events"
export const NODE_PATH = "/node"
export const EXPORT_PATH = "/export"
