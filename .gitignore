node_modules/
dist/
/panel-preview-*.html
.pi/
err.txt
out.txt

# A project-scope MCP registration would land here. Prefer `-s local`, which
# keeps the entry out of the repo and out of other agents' sight:
#   claude mcp add flow-panel -s local -- node ./dist/claude/mcp.js
.mcp.json
